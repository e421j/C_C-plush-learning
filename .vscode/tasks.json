{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "C Build",
            "command": "gcc",
            "args": [
                "-std=c17",
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
