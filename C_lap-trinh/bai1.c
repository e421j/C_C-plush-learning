#include <stdio.h>

// Trình biên dịch: gcc -std=c17 -o bai1 bai1.c
int main(void) { // hàm chính
    printf("Hello World!\n");
    printf("yoo world!!"); // in ra màn hình và "\n" là xuống dòng

    return 0; // trả về kết quả và kết thúc hàm chính
}
// Chạy chương trình: ./bai1exe (Windows) hoặc ./bai1 (Linux/Mac)