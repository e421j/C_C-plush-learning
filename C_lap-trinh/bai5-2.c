// underfined behavior ( hành vi không xác định)
//là sự ngẫu nhiên về giá trị khi in ra màn hình và các giá trị ngẫu nhiên đó đc lấy từ trong hộp hay còn đc gọi là giá trị rác
#include <stdio.h>
int main() {
    int age;
    float gpa;
    char lop;
    char name[30];
    printf("%d\n", age);
    printf("%f\n", gpa);
    printf("%c\n", lop);
    printf("%s\n", name);
    return 0;
}