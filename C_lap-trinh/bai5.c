// Gán giá trị người dùng
// Xác định giá trị người dùng
// k\n: là cấp phát dữ liệu để sử dụng dữ liệu trong khu đó.Chỗ chứu dữ liệu còn đc gội là hộp
#include <stdio.h>
int main() {
    int age;
    float gpa;
    char lop;
    char name[]= "Tien Dung";// Hoặc là sử dụng name[30]=> xác định giới hạn kí tự để gõ
    printf("%d\n", age);
    printf("%f\n", gpa);
    printf("%c\n", lop);
    printf("%s\n", name);
    return 0;
}